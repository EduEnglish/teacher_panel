{
  "name": "eduenglish-notifications-functions",
  "description": "Cloud Functions for the EduEnglish Teacher Panel notifications pipeline.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions",
    "lint": "eslint src/"
  },
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.5",
    "eslint": "^9.36.0",
    "typescript": "^5.3.3"
  }
}

